export default function DUMMY_NEWSNotFoundPage() {
    return(
        <div id="error">
            <h1>Not Found!</h1>
            <p>Unfortunately, we couldn't find the requested article.</p>
        </div>
    );
}